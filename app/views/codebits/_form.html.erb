<%= form_for(codebit) do |f| %>
  <% if codebit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(codebit.errors.count, "error") %> prohibited this codebit from being saved:</h2>

      <ul>
      <% codebit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :code %>
    <%= f.text_area :code %>
  </div>

  <div class="field">
    <%= f.label :language %>
    <%= f.text_field :language %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
